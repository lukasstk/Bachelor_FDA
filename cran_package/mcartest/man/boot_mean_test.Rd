% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boot_test.R
\name{boot_mean_test}
\alias{boot_mean_test}
\title{Bootstrap mean test (L2/Supremum)}
\usage{
boot_mean_test(
  fd = NULL,
  X = NULL,
  groups = NULL,
  observed_ratio = 1,
  n_boot = 10000,
  min_frac = 0.1,
  seed = NULL,
  alpha = 0.05,
  compute_bands = TRUE,
  bands_only = FALSE,
  ncpus = max(1L, parallel::detectCores(logical = TRUE) - 2L),
  stat = c("L2", "D"),
  chunk_size = NULL,
  manage_backend = "auto",
  worker_blas_threads = 1L,
  max_redraws = 20L
)
}
\arguments{
\item{fd}{Optional \code{tfd}/\code{tfd_irreg} (tidyfun). If supplied,
takes precedence over \code{X}.}

\item{X}{Optional numeric matrix (n x m) with NAs (observations).}

\item{groups}{Optional 2-level grouping vector of
length n (logical/character/factor/numeric).}

\item{observed_ratio}{Threshold in \[0,1\] used for auto-grouping
when \code{groups} is missing.}

\item{n_boot}{Target number of bootstrap iterations.}

\item{min_frac}{Minimum per-time-point coverage per group
used to select the subdomain.}

\item{seed}{RNG seed (passed to doRNG).}

\item{alpha}{Significance level for bands (Supremum only).}

\item{compute_bands}{Compute confidence bands (Supremum only)?}

\item{bands_only}{Logical: if TRUE, return only
band information instead of a full \code{htest}.}

\item{ncpus}{Number of workers for an internal cluster (if created).}

\item{stat}{\code{"L2"}, \code{"D"} oder \code{c("L2","D")}.}

\item{chunk_size}{Number of bootstrap replicates per foreach task.}

\item{manage_backend}{Backend control (\code{"auto"}, \code{"force_pool"}, \code{"sequential"}).}

\item{worker_blas_threads}{BLAS/OpenMP threads
per worker (internal pool only).}

\item{max_redraws}{Maximum number of redraw attempts
for invalid bootstrap samples.}
}
\value{
\code{htest} (with extras) or a list of both tests.
}
\description{
Returns bootstrap p-values for L2 or Supremum.
}
\examples{
set.seed(1)
m <- 30
n <- 200
grid <- seq(0, 1, length.out = m)
bm <- function(g) {
  d <- diff(g)[1]
  c(0, cumsum(rnorm(length(g) - 1, sd = sqrt(d))))
}

# Group A: standard BM; Group B: BM with mean shift
X_A <- t(replicate(n / 2, bm(grid)))
X_B <- t(replicate(n / 2, bm(grid))) + 0.3
X <- rbind(X_A, X_B)

# Define groups: FALSE = A, TRUE = B
groups <- c(rep(FALSE, n / 2), rep(TRUE, n / 2))

# MNAR censoring: observed only if -1 < X(t) < 2
O <- 1L * (X > -1 & X < 2)
X[O == 0L] <- NA_real_

# Bootstrap Supremum test
res_boot <- boot_mean_test(
  X = X,
  groups = groups,
  n_boot = 2000,
  stat = "D",
  alpha = 0.05,
  compute_bands = TRUE,
  manage_backend = "sequential",
  seed = 1
)

res_boot$p.value
}
