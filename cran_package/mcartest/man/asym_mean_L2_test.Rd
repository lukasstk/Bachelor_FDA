% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/asym_tests.R
\name{asym_mean_L2_test}
\alias{asym_mean_L2_test}
\title{Asymptotic L2 test for MCAR}
\usage{
asym_mean_L2_test(
  fd = NULL,
  X = NULL,
  groups = NULL,
  observed_ratio = 1,
  fve = 0.99,
  n_sim = 10000,
  min_frac = 0.1,
  seed = NULL
)
}
\arguments{
\item{fd}{Optional \code{tfd}/\code{tfd_irreg} (tidyfun). If supplied,
takes precedence over \code{X}.}

\item{X}{Optional numeric matrix (n x m) with NAs (observations).}

\item{groups}{Optional 2-level grouping vector of
length n (logical/character/factor/numeric).}

\item{observed_ratio}{Threshold in \[0,1\] used for
auto-grouping when \code{groups} is missing.}

\item{fve}{Fraction of variance explained (0-1) to choose \eqn{q}.}

\item{n_sim}{Number of Monte Carlo draws for the KL-mixture.}

\item{min_frac}{Minimum per-time-point coverage per group
used to select the subdomain.}

\item{seed}{RNG seed.}
}
\value{
\code{htest} (with extras).
}
\description{
Tests \eqn{H_0:\mu_A=\mu_B} using the statistic
\deqn{T_{\mu,L^2} = n \, \lVert \hat{\mu}_A - \hat{\mu}_B \rVert^2_{L^2}.}
}
\details{
p-values are obtained from a Karhunen–Loève mixture;
the number of components is determined by the fraction of
variance explained (FVE).
}
\examples{
set.seed(1)
m <- 50
n <- 40
grid <- seq(0, 1, length.out = m)
bm <- function(g) {
  d <- diff(g)[1]
  c(0, cumsum(rnorm(length(g) - 1, sd = sqrt(d))))
}
X <- t(replicate(n, bm(grid)))

# MNAR censoring: observed only if -1 < X(t) < 2
O <- 1L * (X > -1 & X < 2)
X[O == 0L] <- NA_real_

# Asymptotic L2 test
res_L2 <- asym_mean_L2_test(
  X = X,
  n_sim = 2000,
  seed = 123
)
res_L2$p.value
}
